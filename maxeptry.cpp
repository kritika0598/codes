#include<iostream>
#include<bits/stdc++.h>

using namespace std;
typedef long long int lli;

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	lli N,c,s;
	lli coins=1000;
	cin>>N>>c;
	bool flag=true;
	double l=0;
	double r=N;
	lli prev, prevval;
	lli z=100;
	lli y=ceil((l+r)/z);
	lli x=y;
	while(flag && coins>=0)
	{	

	}

	cout<<3<<" "<<x<<endl;
	

		
}