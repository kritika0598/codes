#include<iostream>
#include<bits/stdc++.h>

using namespace std;
typedef long long int lli;
lli Arr[105];
vector<lli
lli foo(lli n)
{
	if(n==N-1)
		return Arr[N-1];

	return min(Arr[n]*)
}

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	lli T;
	cin>>T;
	while(T--)
	{
		lli N;
		cin>>N;
		memset(Arr,0,sizeof(Arr));
		for(lli i=0;i<N;i++)
		{
			cin>>Arr[i];
		}
		foo(0);
	}
}